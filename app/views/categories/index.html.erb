<ul class="media-list">
    <% @categories.each do |category| %>
      <li class="media">
        <div class="media-body">
          <div>
            <%= link_to category.content, category_path(category) %>
          </div>
        </div>
      </li>
    <% end %>
</ul>
